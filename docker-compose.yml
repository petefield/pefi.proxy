services:
  proxy:
    image: petefield/pefi.proxy:latest
    pull_policy: always
    ports:
      - "5053:8080"
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
